# --- Source / IO ---
SRC=0
YOLO_MODEL=yolov8n.pt
YOLO_ENGINE=yolov8n.engine
IMG_SIZE=640
CONF_THRESH=0.60
VID_STRIDE=1
SAVE_DIR=/workspace/work/alerts
DRAW=1

# --- Presence / Tracking / Alerts ---
TRIGGER_CLASSES=person,dog,cat, potted plant
DRAW_CLASSES=person,car,dog,cat,motorcycle
MIN_FRAMES=3
MIN_PERSIST_SEC=1.0
REARM_SEC=20
RATE_WINDOW_SEC=30
TRACKER=botsort.yaml

# --- Adaptive Rate (new modular pipeline) ---
BASE_FPS=5
HIGH_FPS=0           # 0 = uncapped
BOOST_ARM_FRAMES=1
BOOST_MIN_SEC=0
COOLDOWN_SEC=5.0

# --- Optional zone polygon (x,y;x,y;...) ---
ZONE=

# --- Telegram (optional) ---
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
